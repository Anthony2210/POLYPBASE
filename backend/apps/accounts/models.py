"""User profile and role models will live here.

The project currently uses Django's built-in User model. Keep account-related
code in this app when custom profiles or extra permissions are added.
"""
